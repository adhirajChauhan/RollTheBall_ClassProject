using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
/*    public GameObject car;
    public GameObject ground;
    // Start is called before the first frame update
    void Start()
    {
        //transform.position = new Vector3(0, 0, 0);
        car.transform.position = new Vector3(0, 0, 0);
        ground.GetComponent<MeshRenderer>().material.color = Color.black;
    }

    // Update is called once per frame
    void Update()
    {
        car.transform.Rotate(new Vector3(10, 15, 30) * Time.deltaTime);

        //Get the color of the car 
        car.GetComponent<MeshRenderer>().material.color = Color.Lerp(Color.red, Color.yellow, Mathf.PingPong(Time.time, 1));
    }*/
}
